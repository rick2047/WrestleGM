## 1. Workflow
- [x] 1.1 Add a PR workflow that runs `uv run pytest` for all pull requests.
- [x] 1.2 Add a sticky PR comment step that updates on each run.
- [x] 1.3 Set required GitHub token permissions for PR comment updates.

## 2. Validation
- [x] 2.1 Confirm the workflow YAML is valid and the `pull_request` trigger is configured correctly.
