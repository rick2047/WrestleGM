## 1. Workflow
- [ ] 1.1 Add a PR workflow that runs `uv run pytest` for all pull requests.
- [ ] 1.2 Add a sticky PR comment step that updates on each run.
- [ ] 1.3 Set required GitHub token permissions for PR comment updates.

## 2. Validation
- [ ] 2.1 Confirm the workflow YAML is valid and the `pull_request` trigger is configured correctly.
