## 1. Snapshot UI Fixtures

- [x] 1.1 Copy current `data/wrestlers.json` and `data/match_types.json` into `tests/fixtures/ui/` as the snapshot source
- [x] 1.2 Add rivalry seed data to the UI fixtures (e.g., `tests/fixtures/ui/rivalries.json`)
- [x] 1.3 Document a manual refresh step for updating the snapshot when desired

## 2. Update UI Test Fixture Loading

- [x] 2.1 Load rivalry seed data into the UI test state when starting a test session

## 3. Validate

- [x] 3.1 Run `uv run pytest` and ensure tests pass
